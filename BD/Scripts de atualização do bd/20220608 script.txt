



ALTER TABLE CONTAS_RECEBER_BAIXA DROP PESSOA_ID


CREATE TABLE PEDIDOS_PAGAMENTOS (
    ID         INTEGER NOT NULL,
    PEDIDO_ID  INTEGER,
    PAGO_EM    DATE,
    VALOR      DECIMAL(9,2),
    HISTORICO  VARCHAR(255)
);



ALTER TABLE PEDIDOS_PAGAMENTOS ADD CONSTRAINT PK_PEDIDOS_PAGAMENTOS PRIMARY KEY (ID);



ALTER TABLE PEDIDOS_PAGAMENTOS ADD CONSTRAINT FK_PEDIDOS_PAGAMENTOS_1 FOREIGN KEY (PEDIDO_ID) REFERENCES PEDIDOS (ID);
